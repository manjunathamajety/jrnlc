#compiled binaries
*.out

#temporary files
*.txt

#cmake build directory
#this is regenerated every build, so no point in versioning
build/

#docs directory, excluding everything except the pdf file
docs/*
!docs/design.pdf
